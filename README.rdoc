== README


xctrack requires Ruby 2.3.x because of the current rails version. Depending on your platform, you'll also need libpq-dev to install the postgres library for Ruby.

```
rvm install 2.3
ruby --version
gem install bundle
bundle install
```

